# Product Log — groovely

**Workflow version:** 1.0.0 (from the root `VERSION` file at creation)

| Date | Agent | Entry |
|------|-------|-------|
| 2026-08-22 13:25 | navigator | Project created under workflow v1.0.0. Beginning discover phase. |
| 2026-08-22 13:28 | discover | Decision: authenticated users, one private collection each. Music recommendations explicitly post-MVP. |
| 2026-08-22 13:31 | discover | Decision: records added by barcode scan (browser feasibility to be proven) with manual entry always available. |
| 2026-08-22 13:36 | discover | Decision: Discogs is the metadata/barcode lookup source. Tier 2 flagged: API ToS caching clause (no display of content >6h stale, no caching beyond service need) needs an ADR on the copy-on-confirm ownership pattern. |
| 2026-08-22 13:44 | discover | Decision: MVP includes search/filter/sort and wantlist. Pending: condition+purchase tracking and collection stats awaiting clarification. |
| 2026-08-22 13:48 | discover | Decision: MVP includes condition grading (media + sleeve, Goldmine scale) and purchase provenance. Collection stats deferred to out-of-scope.md. |
| 2026-08-22 14:05 | discover | Decision: browser app is the MVP target and is FULLY specified and verified (not an out-of-workflow spike). Meta-goal: prove product-workflow v1.0.0 can carry a product of this complexity. Native apps are a later, separate workflow run. |
| 2026-08-22 14:14 | discover | Decision: self-managed email + password authentication with 2FA. Pending: 2FA mechanism, enrolment policy and account-recovery path (Tier 1). |
| 2026-08-22 14:21 | discover | Decision: 2FA = TOTP (RFC 6238), enrolment mandatory at sign-up, single-use recovery codes as lost-device path. Pending: TOTP + password parameter set (Tier 1). |
| 2026-08-22 15:55 | discover | Decision: password policy = NIST SP 800-63B modern school (min 12, max 128, no composition rules, no expiry, paste allowed) + Have I Been Pwned k-anonymity breach screening, fail-open on HIBP unavailability. TOTP/lockout/session parameters accepted as proposed. |
| 2026-08-22 15:57 | discover | Decision: copy-centric data model — one entry = one physical copy owned, carrying its own metadata, condition and purchase data. Resolves the Discogs ToS caching concern via copy-on-confirm ownership. |
| 2026-08-22 16:31 | discover | Decision: scale target = up to 10,000 records per collection. Drives server-side pagination, indexed search, numeric NFR targets. |
| 2026-08-22 16:32 | discover | Verified BarcodeDetector support: Safari disabled by default (macOS+iOS), Firefox none, Chrome desktop partial, Chrome Android full (~76% global). Baseline must be getUserMedia + WASM decoder over HTTPS. |
| 2026-08-22 17:55 | discover | Decision: barcode scanning specified as one behaviour, implemented via getUserMedia + WASM decoder (zxing-wasm) on all browsers with BarcodeDetector as an opportunistic fast path. HTTPS/secure-context becomes a hard architecture constraint. Manual entry is a first-class path, not a failure fallback (pre-1980s vinyl has no barcode; reissues share barcodes across variants, so a scan may return several candidates to disambiguate). |
| 2026-08-22 18:09 | discover | Decision: MVP includes full collection export (JSON + CSV) and in-app account deletion cascading to all records, wantlist entries, TOTP secrets and recovery codes. Drivers: Apple App Store Guideline 5.1.1(v) (in-app deletion mandatory since 2022-06-30) and UK GDPR Art.15/17/20. |
| 2026-08-22 18:10 | discover | Decision: cover art stored as a Discogs image URL reference (not bytes), rendered from their CDN; collector may upload own photos which take precedence. Requires a broken-image/URL-rot path in the spec. |
| 2026-08-22 14:05 | discover | Decision: browser MVP is ONLINE-ONLY, with every network failure given an explicit observable message and recovery path. Offline support recorded in out-of-scope.md with reason. |
| 2026-08-22 14:05 | discover | Decision (forward constraint): the mobile app WILL hold its own local data store and support offline browsing of the catalogue. The API must therefore be sync-shaped from day one — server-assigned stable ids, monotonic updated_at/version on every entity, soft-delete tombstones, and a delta-pull endpoint shape. Binding on the browser build even though it will not consume it. |
| 2026-08-22 14:25 | discover | Decision: support matrix = mobile-first, latest 2 versions of Chrome/Edge/Firefox/Safari desktop, iOS Safari 16+, Chrome Android. Verify must run a Chromium/WebKit/Gecko matrix (closes finding #5). |
| 2026-08-22 14:25 | discover | data-model.md drafted (8 entities, all fields typed/constrained). Decisions: display_name dropped; no IP/user-agent on sessions; condition grading optional; rating REJECTED (judges the music not the copy - incoherent under a copy-centric model); shelf_location renamed storage_location and reframed to impose no organisational scheme; notes retained; play tracking and tags out of MVP. |
| 2026-08-22 14:40 | discover | Product principles stated: (P1) not a thin client of Discogs; (P2) the data is the collector's; (P3) the app works offline; (P4) the collector's own photograph is the authoritative image. Filed as docs/specs/product-principles.md. |
| 2026-08-22 14:40 | discover | Decision: collector-supplied sleeve photographs are IN the MVP and promoted to core (P4). Discogs cover art demoted to fallback. EXIF stripping mandatory. |
| 2026-08-22 14:40 | discover | CONFLICT SURFACED: P3 (works offline) contradicts the earlier decision that the browser MVP is online-only with offline in out-of-scope.md. Pending: Product Engineer to resolve scope of offline support. |
| 2026-08-22 14:50 | discover | CONFLICT RESOLVED: P3 scoped to the mobile apps. Browser MVP remains online-only. Rationale: prove the workflow first; building the full roadmap in run one risks never completing. Known limitation recorded - the MVP does not exercise the product's differentiator, and local-first sync will be met unrehearsed in the mobile run. |
| 2026-08-22 15:00 | discover | Spec documents drafted: product-principles.md, data-model.md, non-functional.md, security.md, integrations.md, out-of-scope.md. Feature files next. |
| 2026-08-22 15:20 | discover | glossary.md written (BDD standard requires all scenario vocabulary be defined). 6 authentication feature files written: registration, password-length, breach-screening-unavailable, email-verification, authenticator-enrolment, recovery-codes. |
| 2026-08-22 15:40 | discover | 34 feature files written and structurally validated (1 Given / 1 When / 1 Then each, zero And keywords). ui-field-map.md written, satisfying the Tier 1 rule on user-editable fields. standards/bdd.md amended to disambiguate the AND rule (finding #7 closed). |
| 2026-08-22 15:55 | discover | scope.md written at Product Engineer request - plain-language statement of the MVP boundary. |
| 2026-08-22 16:30 | discover | Review feedback applied. (1) 2FA: mandatory TOTP retained, trusted-device capability added (30 days, explicit opt-in, revoked on password change). Corrected a premise - TOTP codes are generated offline and need no network; the offline objection argues against SMS/email codes, not TOTP. (2) Purchase price, currency and seller CUT on the Product Engineer's veracity argument (self-entered price is not evidence of value); acquisition_date retained as a separable, non-financial field. (3) NEW REQUIREMENT: structured personnel credits - Person and Credit entities, roles, primary-artist flag, pre-filled from Discogs extraartists, browse and filter by person, duplicate merge. |
| 2026-08-22 16:30 | discover | Artefacts updated: data-model.md (+Person, +Credit, +TrustedDevice, -purchase block), glossary.md, security.md, integrations.md, out-of-scope.md, scope.md, ui-field-map.md. Feature files: purchase-details.feature superseded by acquisition-date.feature; added credits-from-lookup, credits-manual, credits-person-reuse, browse-by-person, merge-people, trusted-device, trusted-device-expiry, trusted-device-revocation. Now 41 files / 98 scenarios, all structurally valid. |
| 2026-08-24 09:12 | discover | Admission test adopted: a requirement enters the MVP only if it positively serves a stated principle. Anchored to product-principles.md rather than scope.md, which is circular. Track listings tested against it and ADMITTED (printed on the sleeve; the practical means of distinguishing pressings; object identity rather than playback). |
| 2026-08-22 17:10 | discover | Track listings ADMITTED under the admission test. Track entity added (position as side-and-position text A1/B2 with a separate integer sort key - an integer track number is wrong for this medium; title; optional duration; max 200 per record). Credit gains a nullable track_id: null means whole-record, set means the personnel change between tracks. Populated from the lookup's tracklist and its per-track extraartists. Search now spans track titles and every result must state why it matched. |
| 2026-08-22 17:10 | discover | Artefacts updated: data-model.md, non-functional.md (search target now holds against ~150,000 track rows, not 10,000 records), glossary.md, scope.md, integrations.md, ui-field-map.md. Feature files added: track-listing, track-ordering, search-by-track, credits-per-track. Now 45 files / 106 scenarios, all structurally valid. |
| 2026-08-22 17:30 | discover | Track model SIMPLIFIED on Product Engineer review: side char(1) A-Z plus position smallint 1-99, replacing the earlier text position plus separate sort key. Ordering is ORDER BY side, position - no sort column needed, and A2 precedes A10 naturally. Displayed position (A1) is derived, never stored. Stated limit: 26 sides / 13 discs; a larger box set cannot be fully catalogued, declared rather than silently mis-ordered. Manual reorder control removed - track order is a fact about the record, not a preference. |
| 2026-08-22 17:30 | discover | Search result attribution rationale recorded in ui-field-map.md with a worked example. Its purpose is verification teeth, not presentation: "the collector is shown that record" passes even when search is wholly broken; "the result states it matched the track So What" cannot. |
| 2026-08-22 18:00 | discover | Search scope selector added (everything / artist / title / track / person / label / catalogue number). Product Engineer correction applied: a search result is ALWAYS a record - the object is what the application is about, and artist, title and track are attributes it has. Scope narrows what is searched, never what a result is; one result shape for every scope. Proposed release-type field (Album/Compilation/Single) REJECTED as a consequence - it describes the release concept, not the object; the existing format enum is the accurate descriptor. |
| 2026-08-22 18:00 | discover | Falsifiability pass over all 110 scenarios. Ten Then steps were satisfiable by a broken implementation and were rewritten: search-collection (x2), search-by-track, filter-collection (clear), sort-collection (x3), browse-collection (pagination), export-collection, browse-by-person (x2). Dominant defect: the Given lacked a distractor, so the assertion could not distinguish "found the right thing" from "returned everything". Page size fixed at 50 to make the pagination assertion falsifiable. |
| 2026-08-22 18:00 | discover | Two stale/unobservable clauses corrected: view-record.feature still asserted purchase details after they were cut; sleeve-photographs and delete-account asserted internal state ("the stored photograph", "no record remains in Groovely") rather than observable outcomes, contrary to standards/bdd.md. Both restated as observable consequences. |
| 2026-08-22 18:40 | discover | Filter facets reviewed against the admission test. CUT: filter by person (duplicated browse-by-person - two implementations of one result set), filter by media condition (unjustifiable; condition is looked up on a record, not browsed by), filter by decade (justified only by "the field existed"). KEPT: filter by format, restated as matching how a collection physically exists. Search scopes all retained: everything, artist, title, track, person, label, catalogue number. |
| 2026-08-22 18:40 | discover | Catalogue number scope RE-JUSTIFIED: not a recall aid (nobody remembers BLP 1577) but a transcription aid - read off the label of a record in hand to check whether that pressing is already owned. It is the pre-barcode identity check, covering exactly the older records where barcode scanning fails. Distinguishes pressings that share artist and title. |
| 2026-08-22 18:40 | discover | CONDITION GRADING CUT on Product Engineer challenge. Goldmine grades are subjective and inconsistent between graders, decay with play, and - decisively - lost their only consumer when the media-condition filter was cut. A captured field nothing reads is schema, validation, UI, export and verification surface for no function. notes covers the one real use ("player copy, surface noise side 2") more expressively. Removed from data-model, glossary, ui-field-map, scope, non-functional; grade-condition.feature retired; view-record.feature and filter-collection.feature corrected. |
| 2026-08-22 18:40 | discover | Collection view is a collector choice: grid of sleeves or list of rows, remembered between visits. Page size follows the view - 24 in grid, 20 in list. Images below the fold are lazy-loaded, so page size does not constrain the 2.5s cold-paint target and is purely a question of interruption frequency. |
| 2026-08-22 19:00 | discover | Search semantics settled. MATCHING: word-prefix, accent-insensitive, conjunctive (all terms required), over Unicode-normalised case-folded accent-folded text. Chosen partly on performance - word-prefix is served by an ordinary index, substring is not, and p95 <= 300ms must hold across ~150,000 track rows. Minimum query two characters; empty query clears the term and leaves filter, sort and view untouched. |
| 2026-08-22 19:00 | discover | COMBINATION: OR within the format facet, AND between search and filter. Already asserted by filter-collection.feature; search-with-filter.feature added so the AND is asserted rather than inferred. |
| 2026-08-22 19:00 | discover | ORDER: default date added, newest first. Every ordering made total by a final tie-break on record id - without it a sort assertion is unverifiable whenever two records share the sorted value, and rows can duplicate or vanish across a page boundary. |
| 2026-08-22 19:00 | discover | PAGING: Load more button, not infinite scroll - keyboard-operable, announceable to assistive technology, deterministic for verify, and it leaves a stable stopping point. View, scope, search term, filter, sort and page all live in the URL, so back from a record returns the collector to their exact place and a view can be bookmarked. Switching grid/list returns to page one because page sizes differ. |
| 2026-08-22 19:00 | discover | Feature files added: search-matching (6), search-with-filter (2), result-order (2), returning-to-the-collection (3). Now 50 files / 123 scenarios, all structurally valid. |
| 2026-08-24 | sufficiency-check | FAIL. Run adversarially and deliberately out of phase (design incomplete; Step 0 scoped to discover artefacts). Two independent reviewers with no access to the authoring reasoning. 46 Tier 1 findings across 5 groups: 7 inter-artefact contradictions, 9 undefined contracts, 10 unenumerated/unbounded values, 20 falsifiability defects; plus 6 classes of BDD standard violation. See docs/build/sufficiency-report.md. |
| 2026-08-24 | sufficiency-check | TRIAGE (authentication/session cluster). Governing rule adopted: every scenario that ends an authority must be followed by a scenario that attempts to use it. Root cause of findings D1-D5 was that every Then in this area named a SCREEN rather than probing what access remained. |
| 2026-08-24 | sufficiency-check | A1 RESOLVED: removed "There is no trusted-device bypass" from authenticator-challenge.feature, which contradicted trusted-device.feature, security.md and data-model.md s12. |
| 2026-08-24 | sufficiency-check | Glossary actors redefined (also resolves E4): collector is now defined by ACCOUNT OWNERSHIP, not current session state, so a collector who has signed out or is mid-challenge is still the collector. The old definition made the most important scenarios unwritable. |
| 2026-08-24 | sufficiency-check | D1 RESOLVED: two scenarios added asserting the collection cannot be reached before the authenticator challenge is answered, or with no sign in at all. Previously an implementation could issue the session on password success and merely RENDER the prompt - 2FA decorative, suite green. |
| 2026-08-24 | sufficiency-check | D2 RESOLVED: sign-out now probed by navigating back to the collection, not merely asserting a redirect. |
| 2026-08-24 | sufficiency-check | D3 RESOLVED: trusted-device revocation now asserts that the revoked device is CHALLENGED, not merely absent from a list; a positive listing scenario added (previously an always-empty list passed both scenarios); the unticked-by-default trust choice now has a scenario. |
| 2026-08-24 | sufficiency-check | D4 RESOLVED: erasure now probed - a deleted account's sleeve photograph must no longer be served at its address, and a second signed-in device must lose access. |
| 2026-08-24 | sufficiency-check | D5 RESOLVED (and E1 in the same edit): password-reset.feature rewritten - the new password must work, the replaced password must fail, breach screening applies on reset, and the two-action When was split. |
| 2026-08-24 | sufficiency-check | A3 RESOLVED: the uniform-error rule and the lockout message now coexist. A locked account is told it is locked ONLY when the submitted password is correct - disclosure is gated on knowledge an enumerating attacker does not have. account-lockout.feature rewritten to 8 scenarios asserting both branches plus the unregistered-address case. |
| 2026-08-24 | sufficiency-check | B7 RESOLVED: password change specified. Requires the CURRENT password - an active session alone is not sufficient authority. Breach screening applies; all sessions and all trusted devices revoked, including the session making the change. change-password.feature added (6 scenarios), including that the replaced password stops working and the changing session itself loses access. |
| 2026-08-24 | sufficiency-check | D6 RESOLVED: the lock always ends. 15 minutes, counter resets to zero on expiry, attempts during a lock do not extend it. Fixes a real defect - as previously written the counter reset only on success, so a lapsed lock would re-lock on the next single typo indefinitely, with no administrator in the product to undo it. Scenarios assert the lock ending and the counter having reset. |
| 2026-08-24 | sufficiency-check | A2 RESOLVED: the person-filter scenario deleted from browse-by-person.feature. It mandated a capability out-of-scope.md excludes, so a conforming build failed the scenario and a passing build failed the verify gate. |
| 2026-08-24 | sufficiency-check | A6 RESOLVED: stale references to "condition and purchase details" corrected in duplicate-records.feature and data-model.md s5/s6 - both fields were cut, and the prose could have led a build agent to reinstate them, which fails the gate. |
| 2026-08-24 | sufficiency-check | A7 RESOLVED: all three [DECIDE] markers cleared. display_name deleted (already cut in out-of-scope.md); session IP/user-agent confirmed not stored; sleeve photographs confirmed IN and core per P4, overruling the earlier drafting recommendation. No decision markers remain in any artefact. |
| 2026-08-24 | sufficiency-check | A4 RESOLVED: account erasure HARD-deletes. Account.deleted_at removed - tombstones exist so a future mobile client can learn of deletions during sync, and a deleted account has no client left to sync. Removing the row releases the email so the collector may register again; exercising an erasure right must not carry a permanent penalty. |
| 2026-08-24 | sufficiency-check | A5 RESOLVED: disc_count stays 1-20. A box set of any size is catalogued in full; only the TRACK LISTING is limited to sides A-Z, and the collector is told so at the point it applies. The limitation is on one optional field, not on cataloguing the object. box-set-track-limit.feature added. |
| 2026-08-24 | sufficiency-check | GROUP B RESOLVED (9 findings). B1: export contract written (docs/specs/export.md) - a zip of collection.json, per-entity CSVs, the photograph FILES, and a README. Photographs included as files because hosted URLs stop resolving on account deletion, which is exactly when an export matters; a reference-only export would discharge P2 in name only. Credentials never appear; tombstoned rows excluded; 2 GiB cap; synchronous download so a data right never depends on mail delivery. |
| 2026-08-24 | sufficiency-check | B2/B3: transactional email and object storage declared as the third and fourth external dependencies, specified provider-agnostically with failure modes. Mail failure must never block registration and the visitor-facing message is unchanged so it cannot become a delivery oracle. Photograph upload failure still saves the record. |
| 2026-08-24 | sufficiency-check | B4: VerificationToken and ResetToken entities added - Argon2id-hashed, single use, verification 24h (previously unstated entirely), reset 60min. Expired and consumed tokens are indistinguishable to the visitor so neither becomes an oracle. |
| 2026-08-24 | sufficiency-check | B5: track position parse rule specified. Letter-plus-digits parses; anything else is offered to the collector with side and position BLANK for them to complete, and nothing saves until valid. No track silently dropped, no side or position ever invented. |
| 2026-08-24 | sufficiency-check | B6: person matching on lookup - external artist id first, then exact case-and-accent-folded name, else create. A contradicting external id under the same name creates a SECOND person: two musicians sharing a name is not hypothetical in jazz, and surfacing a real distinction beats silently collapsing two people. |
| 2026-08-24 | sufficiency-check | B8: email made NON-EDITABLE in the MVP. It was marked editable with no flow, no re-verification rule and no enumeration-safe errors. Changing an email address is a genuine account-takeover surface not worth opening for a private catalogue. |
| 2026-08-24 | sufficiency-check | B9: error taxonomy written (docs/specs/errors.md) - trigger, copy, retry and resulting state for twelve error classes, plus universal rules. Connection loss specified as an operating condition: persistent banner, reads continue, writes blocked with a clear message, and INPUT PRESERVED IN FULL. Losing a half-typed record to a dropped signal is the failure most likely to make someone abandon the app. |
| 2026-08-24 | sufficiency-check | Feature files added: export-collection rewritten (6), connection-loss (5), track-position-parsing (4), credits-person-matching (4), email-verification expiry (1). Now 55 files / 165 scenarios, all structurally valid. |
| 2026-08-24 | sufficiency-check | GROUP C RESOLVED (10 findings). C1: the 15 genre values written out literally, VERIFIED against the Discogs database guidelines rather than recalled - the recalled version had "Hip Hop" where Discogs writes "Hip-Hop". A database enum cannot be built from a reference to a third party's list. |
| 2026-08-24 | sufficiency-check | C2: supported currencies enumerated (GBP EUR USD JPY CAD AUD CHF SEK NOK DKK NZD). Decimal places now follow the ISO 4217 EXPONENT rather than a fixed two - a hardcoded two would silently multiply every yen amount by a hundred. |
| 2026-08-24 | sufficiency-check | C3: "Everything" scope defined as artist, title, label, catalogue number, track title and credited person. Product Engineer OVERRULED the recommendation to include notes and storage location - judged too broad. Those two fields are consequently not searchable by any scope in the MVP. A dedicated Notes scope was offered as a follow-up. |
| 2026-08-24 | sufficiency-check | C4: max_price bounded 0-99,999,999 minor units rather than left to the accident of the integer type. C5: barcode fixed to exactly 8, 12 or 13 digits (EAN-8 / UPC-A / EAN-13) with a check digit valid for that length - the earlier 8-14 range admitted lengths matching no named symbology, for which "checksum-valid" had no meaning. |
| 2026-08-24 | sufficiency-check | C6: 10,000 records is a BENCHMARK, not a ceiling. Nothing rejects the 10,001st record; beyond that volume performance is untested rather than forbidden. Scope wording changed from "up to" to "at least". |
| 2026-08-24 | sufficiency-check | C7: collection view stored as Account.preferred_view (enum grid/list, default list). Browser storage would have made "remembered between visits" only conditionally true - true on one browser, false on a second device - and a conditionally true assertion is unverifiable. It also travels to the mobile apps for free. |
| 2026-08-24 | sufficiency-check | C8: sort direction given a default per key (text ascending, dates descending), a URL parameter, and a place in the controls table - it was previously an orphaned table row detached from any table, with no default, so a scenario asserting "ordered by artist" passed in either direction. |
| 2026-08-24 | sufficiency-check | C9: nulls sort LAST in both directions. A total order over the non-null rows is not a total order over the collection, and the database default differs by direction so it would otherwise have been inherited by accident. |
| 2026-08-24 | sufficiency-check | C10: artist collation - ignore a leading The/A/An, otherwise sort as written under ICU en_GB. Surname inversion deliberately rejected: software cannot distinguish a person from a band, and the rule yielding "Davis, Miles" also yields "Report, Weather", failing silently so the record is simply not where the collector looks. |
| 2026-08-24 | sufficiency-check | Feature files added: sort-edge-cases (5), barcode-validation (3). Now 57 files / 173 scenarios. |
| 2026-08-24 | discover | PRINCIPLE P5 ADDED, stated by the Product Engineer: "The app is not supposed to be a replacement for Discogs, where this value can be set. It is instead about the joy of collecting vinyl records and cataloguing that collection." This is the positive statement P1 was missing - P1 said what Groovely is NOT. |
| 2026-08-24 | discover | Wantlist max_price and ALL currency handling cut under P5. A price ceiling is marketplace equipment; the marketplace already exists elsewhere. Removed: max_price_minor, max_price_currency, Account.preferred_currency, the supported-currency list, the ISO 4217 exponent rule, the currency control, and the money row in the export contract. No entity carries an amount. |
| 2026-08-24 | discover | Admission test sharpened with a second razor: "does it serve the joy of collecting, or is it machinery for trading?" It would have rejected max_price on sight, without any argument about currency exponents. Cutting a requirement now also triggers review of what existed only to support it (workflow finding #19). |
| 2026-08-24 | sufficiency-check | GROUP D RESOLVED (remaining 13 falsifiability findings). 174 -> 215 scenarios. |
| 2026-08-24 | sufficiency-check | D7: photograph handling. Non-image file now carries an IMAGE EXTENSION so an extension-only check fails it (security.md forbids exactly that check, and the old scenario could not tell the two apart). EXIF scenario now asserts the served image retains its DIMENSIONS as well as losing its location metadata - previously satisfied by storing nothing and serving a 1x1 placeholder. Size limit, count limit, removal and cross-tenant serving now have scenarios. |
| 2026-08-24 | sufficiency-check | D8/D9: the vacuous-only class. "Shown only records with a format of seven inch" is TRUE OF ZERO RECORDS, so a filter returning the empty set passed. Every such assertion now carries cardinality ("exactly those two"). search-with-filter gained a cross-format distractor - without one, an implementation discarding the filter on search returned the same rows and passed the very scenario written to prevent it. |
| 2026-08-24 | sufficiency-check | D10: the accent scenario contained no accent - "Jobim" is unaccented, so the sole test of accent folding passed with no accent folding whatsoever. Replaced with Dvorak in both directions, each with a distractor. |
| 2026-08-24 | sufficiency-check | D12: person reuse now asserted via the People view count. "Crediting the existing person rather than a new one" is not black-box observable - a reused person and a duplicate render identical text. D13: camera-unavailable asserted manual entry, which is present unconditionally, so the Then was true before the When and independent of the Given; it now asserts the unavailability message. |
| 2026-08-24 | sufficiency-check | D14: a record must now actually be addable while the lookup is down - previously only that a link was OFFERED. Partial-payload path added. D15: confirming a candidate now asserts label, catalogue number and release year were COPIED (previously a blank record with placeholder artist and title passed), plus a scenario that a saved record never re-fetches. |
| 2026-08-24 | sufficiency-check | D16: pagination. There was NO Load more scenario at all - nothing proved page two existed or differed. Total ordering was tested over three records, which never cross a 20-record page boundary, i.e. exactly the defect it exists to catch could not occur. Now asserted over thirty and fifty records. |
| 2026-08-24 | sufficiency-check | D17: changes-persist.feature added. No mutation scenario reopened a record, so an optimistic client update whose write silently failed looked identical to a successful save. Five scenarios now reopen from the collection on a later visit, including that a removed record leaves the search index. |
| 2026-08-24 | sufficiency-check | D18: leader/sideman. An implementation labelling every credit "credited artist" passed - no distractor contributor had to remain one, and nothing asserted the false default. Both now covered. D19: cross-tenant isolation was asserted at exactly ONE route; search, people, wantlist and export each run their own query and now each have a scenario. |
| 2026-08-24 | sufficiency-check | D20: specified surface that had no scenario at all now covered - search scopes person/label/catalogue number, sort by title and release year, match attribution for label/catalogue/person matches, wantlist priority and its default, the 128-character password maximum, the 30-day idle and 90-day absolute session caps, per-track credits arriving from a lookup, and recovery codes being displayed once and superseded on regeneration. |
| 2026-08-24 | sufficiency-check | GROUP E RESOLVED (6 classes). E1: five genuine two-action When steps split - "scans a barcode and waits ten seconds", "opens a record and then goes back", "registers again and reaches their collection", "sorts by artist and loads more records", "submits an address and a password five times". The remaining ~28 conjunctions in When steps are parameters of ONE action (an email address and a password are one form submission) and are permitted under the amended standard. |
| 2026-08-24 | sufficiency-check | E2: four genuine two-outcome Then steps reworded to a single outcome - "shown a message stating X and offered manual entry" became "shown a message offering manual entry because X", which is one thing on screen rather than two. The rest are facets of one outcome (one record view, one archive, one confirmation) and are permitted. |
| 2026-08-24 | sufficiency-check | E3: two passive When steps fixed. "When the collector is asked to confirm the merge" made the collector the object rather than the actor and named no boundary action; "When the collector is shown the message" likewise. |
| 2026-08-24 | sufficiency-check | E4: already resolved when the glossary redefined collector by account ownership rather than session state. E5: glossary extended with release attributes (format, label, catalogue number, release year, side, position, barcode) and flows (registration form, verification link, password reset link, current password, external music database, breach screening service, archive, priority, match attribution). "primary artist" renamed "credited artist" to match what scenarios actually say, and "contributor" defined as its counterpart. |
| 2026-08-24 | sufficiency-check | E6: scenarios now use the enumerated format values (7", 12" single, LP) rather than prose labels ("seven inch"). Verification locates controls by accessible name, so the display label is load-bearing; it is now fixed in ui-field-map.md rather than left to the build to invent. |
| 2026-08-25 | sufficiency-check | FAIL (v2). Re-scan after remediation, two independent adversarial reviewers. 24 Tier 1 ambiguity findings and 20 falsifiability findings. HEADLINE: remediation closed 46 findings and INTRODUCED at least 8 - a ~17% regression rate on a purely textual artefact. Three habits: one-sided edits (a rule corrected where cited, left stale where not); new prose against unreconciled schema; scenarios written against numbers fixed elsewhere in the spec. Worst case R1 - sort-edge-cases.feature now asserts the WRONG order, so a correct build fails it and an incorrect one passes: the suite enforces the defect it was written to prevent. See docs/build/sufficiency-report-v2.md. |
| 2026-08-25 | sufficiency-check | v2 REMEDIATION. Page size set PROVISIONAL at 10 list / 12 grid, reverting an unflagged drift from the Product Engineer's stated instinct of 10; the number is explicitly pending exploratory validation on a real device (workflow findings #22, #23). |
| 2026-08-25 | discover | R1 RESOLVED: artist filing names. New ArtistFilingName entity, owner-scoped, keyed on the artist string - set once, applies to every record by that artist, present and future. Sort key is filing_name if set, else the article-stripped artist, ICU en_GB. Automatic surname inversion still rejected: no reliable signal distinguishes Miles Davis from Weather Report, and the failure is silent. Fixed the INVERTED scenario that demanded Chet Baker before The Beatles - a conforming build failed it and a defective build passed. |
| 2026-08-25 | discover | SEARCH SIMPLIFIED on Product Engineer instruction: "the user should specify whether they are searching for an album or for an artist". No blended Everything scope. Scopes are artist (default), album, track, label, catalogue number; every term must match within a SINGLE VALUE of the chosen attribute. This dissolves the cross-attribute conjunction contradiction rather than resolving it, removes the combined search document and the six-index intersection, and makes every search scenario's expected result obvious. |
| 2026-08-25 | discover | PHOTOGRAPHS reduced to two NAMED SLOTS, front and back. Naming them dissolves the ordering problem by construction - no primary-image rule, no position column, no reorder control, because front is front. The back slot kept deliberately: on a jazz record the back sleeve carries the liner notes and personnel, which is what P5 says the app is for. |
| 2026-08-25 | discover | WANTLIST CUT under P5. It records what the collector does NOT own, while Groovely is about the collection they have; and Discogs' wantlist works only because it is wired to a marketplace Groovely cannot and should not replicate. Removed: the entity, promotion flow, priority vocabulary, four feature files, the export section, an unspecified UI surface and a 2,000-row scale target. |
| 2026-08-25 | sufficiency-check | Introduced-defect fixes: R4 person uniqueness relaxed to (owner, folded name, external id) so two Bill Evanses are possible; R5 data-model searched-attribute list reconciled; R6 scope.md ceiling wording fixed; R7 duplicate sort-direction row removed; R8 ownership convention restated - Track, Credit and RecordImage are owned TRANSITIVELY through their Record and carry no owner_id, which the previous wording asserted they did. |
| 2026-08-25 | sufficiency-check | Falsifiability sweep across the three named habits: positive controls added for photographs served, notes displayed and durations parsed (each was an absence assertion with nothing proving the thing could ever be shown); both sides of every threshold now asserted (session at 29 days, device trust at 29 days, four failures not locking, 128-character password accepted, two-character search); and Givens that already satisfied their Then rewritten (track ordering now inserts out of order, search-clearing now changes the visible set, both person-matching reuse branches now assert a count of two). |
| 2026-08-25 | sufficiency-check | A consistency sweep was run after editing - grepping every removed term across all artefacts - which caught six stale references before they became findings. This is the direct countermeasure to the v2 headline (one-sided edits) and is recommended as a standing step after any remediation. |
| 2026-08-25 | sufficiency-check | FAIL (v3). 30 Tier 1 ambiguity findings, 20 falsifiability findings. HEADLINE: the remediation loop is NOT CONVERGING - regression rate 17% (v2) then 27% (v3). v3 finding [5] is v2's R5 with the documents swapped; v3 finding [19] is v2's R7, which was LOGGED AS FIXED AND NEVER FIXED. Second false completion entry in this log; the first was a finding status closed by a script whose match silently failed. The inverted-scenario class logged as workflow finding #21 recurred immediately in search-matching.feature. See docs/build/sufficiency-report-v3.md. |
| 2026-08-25 | sufficiency-check | CORRECTION to earlier entries: the v2 log entry claiming "R7 duplicate sort-direction row removed" was FALSE - the row was still present until now. Six defects verified and fixed this round, each confirmed by re-reading the file rather than trusting the edit script's output. |
| 2026-08-25 | discover | CONSISTENCY GATE BUILT, in the agent/skill idiom rather than as a standalone tool. New skill .claude/skills/spec-consistency (SKILL.md, references/regression-patterns.md, scripts/check_consistency.py, scripts/apply_resolution.py) following the spec-sufficiency pattern. Two new spec artefacts: docs/specs/constants.md (43 constants, single authoritative source, with word forms and derived boundary forms recorded) and docs/specs/retired-terms.md (12 retirements). validate-artefact.sh extended with a BLOCKING consistency check; settings.json matcher widened Write -> Write|Edit|MultiEdit. sufficiency-check agent amended: Step 0.5 consistency gate, Step 4 resolutions applied through a verifying tool and re-checked after EACH resolution, three new Non-Negotiable Rules. CLAUDE.md skill catalogue and hook documentation updated. |
| 2026-08-25 | discover | Product Engineer ruling: RETIRED is not the same as OUT OF SCOPE. Out of scope is a postponement - the concept remains valid and may return. Retired is abandonment on principle; the vocabulary is meaningless and its reappearance is a defect. Two separate registries, not one. |
| 2026-08-25 | discover | Product Engineer ruling: the consistency check BLOCKS rather than warns. This forces an order of operations, both counter-intuitive and therefore documented: changing a constant means editing constants.md FIRST then its dependants; retiring a term means purging it everywhere FIRST then registering it. |
| 2026-08-25 | discover | Checker proven by MUTATION TEST before being trusted - artefacts copied, one defect of each class injected (retired term, contradicted constant, orphaned glossary term), all three caught, unmutated copy clean, copy discarded. The same standard verify applies to itself. The first draft of the checker produced 351 findings, almost all false; a noisy check is worse than no check because it trains everyone to ignore it. Rebuilt with spec-artefact scoping, date stripping, unit proximity, word-boundary probes, pooled probe values, section-aware cut records and an explicit pragma. |
| 2026-08-25 | discover | UPSTREAM.md written at the workflow root - the changes, the measurement motivating them, integration notes, risks, verification performed, and what was deliberately NOT built. For the product-workflow agent stream to integrate into the master template. |
| 2026-08-25 | sufficiency-check | WAIVER: a numeric availability target is intentionally omitted from non-functional.md. Rationale: verify is a one-shot clean-room conformance gate and structurally cannot establish a time-averaged figure such as 99.9% monthly; asserting a number the terminal gate cannot check would be spec theatre. Availability belongs to an operational monitoring obligation outside verify's remit. Approved by Product Engineer. |
| 2026-08-26 | discover | VOCABULARY DEFECT, Product Engineer surfaced. `cover image` named its subject by which face of the sleeve it showed while actually distinguishing it by whose picture it was; alongside `front photograph` and `back photograph` it read as a third face. RENAMED to `catalogue image` (`cover_image_url` -> `catalogue_image_url`) across glossary.md, data-model.md and sleeve-photographs.feature; `cover image` registered as retired so its reappearance blocks. New glossary term `display image` with a fixed precedence — front photograph, else catalogue image, else back photograph, else no image and no placeholder — stated in ui-field-map.md and asserted by two new scenarios. Logged as workflow finding #28: every mechanical check passed, and only a reader's question found it. |
| 2026-08-26 | discover | CHECKER DEFECT found while applying the above. Retired-term and glossary matching were LINE-SCOPED, so a multi-word term wrapped across a line break passed a BLOCKING gate. Fixed: glossary matching normalises whitespace; retired matching adds a two-line window restricted to boundary-spanning matches whose next line carries no cut-record or pragma exemption of its own — the first attempt at this fired four false positives on blank lines preceding legitimate cut records. Four mutations added (wrapped caught, unwrapped still caught, wrapped glossary use not reported unused, genuinely unused term still reported). Logged as workflow finding #29: a mutation suite proves teeth only against the defect shapes it injects, and one authored by the checker's own author injects the shapes it already handles. |
| 2026-08-26 | discover | PHOTOGRAPH STORAGE RULED, Product Engineer. Groovely stores its OWN rendition and never the uploaded file: bounded to 2000 px on the longest edge (C-040, PROVISIONAL), aspect ratio preserved, never upscaled, re-encoded to JPEG at quality 80 (C-041, C-078). C-037/C-038 reworded as UPLOAD limits. New derived constants C-042 (~0.65 MB per photograph) and C-043 (~1.3 MB per record). 2000 px chosen over a true thumbnail because it is the smallest bound at which liner notes on a twelve-inch back sleeve stay readable — the stated justification for the back slot existing at all — so the constant is load-bearing, not arbitrary. Export cap C-056 unchanged; the breach point moves from ~103 records to the low thousands, where the existing data-only fallback is a real route rather than a theoretical tail. Six scenarios added (over the bound, one pixel over, exactly at, under and not enlarged, aspect preserved, PNG stored as JPEG); the EXIF scenario had its size half removed, since it duplicated the new ones. Reviewed live before the constant is pinned, per PROVISIONAL. |
| 2026-08-26 | discover | INVERTED SCENARIO CAUGHT BEFORE IT LANDED. sleeve-photographs.feature asserted a twelve-hundred-pixel photograph is shown back at twelve hundred pixels — true before the resize rule, and under any resize rule it would have demanded the opposite of the spec, so a conforming build fails and a defective one passes. This is the finding #21 class, caught this time by reading the scenario against the rule being introduced rather than by any check. |
| 2026-08-26 | discover | TWO FURTHER CHECKER DEFECTS, both found by the gate firing on honest work. (1) A CONSTANT REFERENCE WAS READ AS A VALUE: 'C-043 of photographs per record' was parsed as the value 43 and reported as contradicting C-036 — the checker punished precisely the behaviour the registry exists to encourage, citing an ID instead of restating a number. Constant references are now stripped before any number is read. C-056 had escaped only by luck, having no unit adjacent to it. (2) C-040 had NO PROBE, so a contradicted stored-photograph bound passed silently; a probe was added on 'longest edge' rather than on 'pixels', because feature scenarios legitimately state many pixel values and a probe on the unit would fire on all of them. UNITS extended with MB and pixel; NUMWORDS extended and reordered so longer forms match first. Eight mutations run: contradicted numeral caught, contradicted word form caught, correct numeral and word form and boundary form all silent, constant reference beside a unit silent, a real wrong value beside a constant reference still caught, old probes still have teeth. |
| 2026-08-26 | discover | OPEN, RECORDED NOT DECIDED: the collection list must not transfer photographs at stored size — a grid page of C-002 tiles at C-042 each would miss the cold-start target on its own. The obligation and its target are fixed in non-functional.md; the MECHANISM (rendition generated on request, or at import as a second stored object with the schema surface Rule 7 then demands) is left to design deliberately, so it must be answered rather than discovered. |
| 2026-08-26 | discover | SCOPE CUT, Product Engineer. ONE IMAGE PER RECORD for MVP: the collector's own sleeve photograph, else the catalogue image from a lookup. The back-sleeve photograph and the two-named-slot model are cut. Rationale, in his words: Groovely is a CATALOGUE, NOT A DIGITAL REPLACEMENT FOR THE RECORD ITSELF. Personnel are already captured as structured credits; notes lets the collector append anything else about their copy and stays at 2000 characters, deliberately - it does not replace liner notes. Recorded in out-of-scope.md as POSTPONED, not retired, per his own distinction: additional images may return and would be designed afresh. Added to product-principles.md as a third razor, which is sharper than the two already there. |
| 2026-08-26 | discover | CASCADE OF THE CUT. RecordImage loses its slot column; the partial unique constraint becomes at most one live row per record. C-036 2 -> 1. C-040 2000 -> 1000 px (the 2000 px bound existed solely for liner-note legibility, which died with the back slot); still PROVISIONAL but now answering a weaker and different question - sleeve looks right at full width against grid-page transfer cost. C-042 0.65 -> 0.19 MB. C-043 DELETED as an identity constant once there is one image per record. C-073 photograph slot enum removed. Export photograph naming {record_id}-front.jpg -> {record_id}.jpg. Export cap C-056 no longer reached inside the benchmark C-003, so the data-only fallback returns to being a genuine tail case; kept specified because C-040 is provisional. sleeve-photographs.feature rewritten, 18 -> 16 scenarios. The checker probe and NUMWORDS were moved with C-040 in the same batch, or the registry edit would have blocked on its own word forms. |
| 2026-08-26 | discover | THE GRID TRANSFER QUESTION IS DEFERRED, NOT ANSWERED, AND SAID SO. With one image at C-042 a grid page is well inside what lazy loading can carry, where two at the old bound was not. non-functional.md now records that a second smaller rendition is the obvious fix IF measurement shows the cold-start target missed, and names what it would cost (schema surface under Rule 7, its own constants, its own scenarios, a second object erasure must destroy). Framed so the live review answers it with a measurement rather than the argument being had again from first principles. |
| 2026-08-26 | discover | TWO WORKFLOW FINDINGS OPENED. #32: an expensive design argument is DIAGNOSTIC of an unjustified requirement. The back photograph passed the admission test honestly, then dragged a pixel-density argument, a thumbnail-derivative argument and a schema-surface argument behind it - all of which dissolved when the requirement was withdrawn in one sentence. The admission test asks whether a requirement serves a principle, never whether its cost is proportionate, and the cost is invisible at admission time. #33: a POSTPONED capability leaves its vocabulary live and unprotected - front photograph, back photograph and photograph slot are now in neither registry, so a build agent could reintroduce them and no gate would fire. Accepted knowingly rather than misregistering a postponement as an abandonment. |
| 2026-08-26 | discover | CORRECTION, Product Engineer. The finding #33 entry above claimed a build agent could reintroduce a back photograph with no gate firing. FALSE. Out of scope means it is not implemented, and Rule 7 makes that mechanical: an unrequested capability or stored field fails verify. The implementation case was already covered by the terminal gate and the finding misread the absence of a WRITE-TIME check as the absence of ANY check. #33 rewritten and downgraded Medium -> Low. What survives is narrower: a future SPECIFICATION agent writing a back photograph into data-model.md would make it specified, after which verify accepts it as requested surface and the postponement is reversed unnoticed - the R-2 regression shape applied to an out-of-scope decision. Recommended remedy is a sufficiency-check pattern, not a third registry state. Recorded rather than edited into the original, per this log's convention after the two false completion entries. |
| 2026-08-26 | discover | TRACKER RECONCILED AGAINST ARTEFACTS, not against memory or the tracker's own claims. Eight items carried a stale status: 2, 15, 16, 23, 24 and 38 were applied in round 1 but never marked; 30 was DISSOLVED by the one-image cut rather than resolved. Two live defects were found by the reconciliation itself. (1) Item 25 was recorded as open and was HALF FIXED: two of four Givens in password-length.feature still said 'the collector has no Groovely account', a contradiction in terms, while the other two had been corrected - a one-sided edit inside a single file, the R-1 shape at its smallest scale. (2) errors.md carried TWO 'Recovery' rows in the connection-loss table, one saying the banner clears on the next successful request with no polling and the other saying it clears when connectivity returns - which implies the polling the first row denies. An insert that never removed what it replaced. Both fixed. Remaining: 12 decisions plus 2 rulings awaiting (decade filter, collection statistics). |
